body: >
  <h3>Creating New Todos with a Text Field (cont)</h3>

  <p>Now that we have defined our view, let’s update the template to use our new view subclass. All we need to do
  is swap <code>Todos.CreateTodoView</code> for <code>SC.TextField</code>.</p>

codeTarget: 'template'

code: |
  {{view Todos.CreateTodoView id="new-todo" placeholder="What needs to be done?"}}

replacesCode: |
  {{view SC.TextField id="new-todo" placeholder="What needs to be done?"}}

validator: |
  // TODO: Write this
